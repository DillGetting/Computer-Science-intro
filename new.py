''' 1. take in a format string and produce a formatted string
    2. myformatstring() take s a format string and list of valures, returns formatted string


def myFormatString(fstring, values):
    pass

def parseString(fstring):
    pass


    3. parsedstring() takes a format string and returns parse tokens
    4. process parse tokens and build formatted string
    parseToken functions
    need test

def testSuite():
    asser(myFormatString("hello %{i}
                         tokens the format the space the space the format
